{
    "1": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((1*1)*(8*6))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "2": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((1+1)+(6+11))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "3": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((1*1)*(2*8))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "4": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (8*(4+(1+1)))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "5": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((8*6)-(6+6))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "6": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((11*1)*(2*12))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "7": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((1*3)*(2*6))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "8": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((1+1)+(7+12))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "9": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((1*2)+(10+10))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "10": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (22*(1+(1*1)))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "11": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (8*(8+(1*1)))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "12": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (8*(4-(11*1)))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "13": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (7*(5-(1+1)))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "14": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((4*6)+(10-11))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "15": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (4*(3-(1*1)))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "16": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((8*2)/(2/12))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "17": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((4*1)/(1+1))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "18": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((4*1)*(12+12))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "19": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((1*3)/(3/8))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "20": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((1+1)*(2*3))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "21": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (8*(11+(1*1)))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "22": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((1*2)*(3*7))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "23": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((8-11)+(12+12))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "24": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((10*7)/(7/8))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "25": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((5+13)-(1+1))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "26": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((7*12)/(1*2))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "27": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (9*(3+(1*1)))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "28": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (3*(4-(1*1)))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "29": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (12*((2+7)-12))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "30": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((4*6)+(3-7))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "31": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (5*(13-(1*1)))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "32": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (6*(5-(2*1)))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "33": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((1*1)*(11+12))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "34": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((1*10)+(6+12))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "35": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (11-(12*(4-5)))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "36": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((4*11)+(13-13))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "37": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (1+((12*12)/12))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "38": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((2*11)/(11/11))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "39": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((1*4)/(4/6))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "40": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((10+1)*(1+11))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "41": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((1*1)+(5+12))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "42": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((4*7)/(7/12))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "43": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((1*4)+(7+12))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "44": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((6-10)+(12+12))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "45": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((1*10)+(8+8))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "46": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((12-2)+(3*8))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "47": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((2*9)/(9/5))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "48": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((3+11)/(11/12))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "49": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((3*8)+(10-13))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "50": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((5-9)+(12+12))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "51": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((7*5)-(1*1))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "52": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((12*12)/(12+3))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "53": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (4*(5+(6*1)))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "54": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (11*(8-(1*6)))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "55": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((4-8)+(12+12))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "56": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((3*8)+(5-11))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "57": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (9-(12*(5-6)))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "58": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((11+2)+(12/12))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "59": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((11+12)+(13-13))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "60": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((102+13)-(1*1))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "61": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (11*(5-(1*3)))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "62": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((5*5)-(7/12))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "63": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((1-1)*(9-12))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "64": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((2*3)/(3/9))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "65": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((3*4)+(4+1))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "66": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((3*8)+(7-10))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "67": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((3*4)+(9-9))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "68": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((2*5)/(5/8))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "69": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (13+((2*11)/11))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "70": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((6*12)+(13-13))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "71": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((7/7)+(11+9))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "72": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (3*(7+(8*1)))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "73": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((6*4)+(10+10))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "74": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((11-4)+(12+12))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "75": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (4+(4*(1*3)))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "76": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((5*5)+(6-12))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "77": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (6*(5-(1*2)))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "78": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((2-2)+(4*4))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "79": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((7+4)-(1-6))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "80": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((1*6)*(12-9))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "81": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (7-(12*(6-7)))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "82": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((1+10)+(10+10))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "83": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (10*((2*3)-3))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "84": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((3*5)/(5/9))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "85": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((10+1)*(13-1))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "86": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((9*12)/(2*3))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "87": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((2+4)+(7+12))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "88": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((5-8)+(11+13))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "89": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (4*(5+(1*3)))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "90": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (7*(12-(1+8)))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "91": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((3*11)*(8-7))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "92": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (12-(12*(7-10)))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "93": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((9/9)+(2+12))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "94": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (42*(5-(1+2)))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "95": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((2+7)+(7+11))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "96": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((4-4)+(4+13))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "97": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (4*(2-(1*1)))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "98": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((1*1)+(10+5))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "99": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((4*7)+(6-6))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "100": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((7*7)-(10*5))\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response."
}