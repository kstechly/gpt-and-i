{
    "1": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((6 / (1 / 4)) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "2": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (11 / 1) * (11 - 1) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "3": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((8 - 1) * 3) * 1 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "4": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((1 + 1 + 1) * 8) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "5": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (6 / 6) * (6 + 6) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "6": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (1 + 1) * 2 * 12 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "7": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (6 / (1 - (2 / 2))) * 4 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "8": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (12 / (1 + 1)) * 10 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "9": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (10 * 2) - (2 * 2) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "10": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (12 * (1 + 1)) + 1 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "11": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((8 / 1) * (2 + 1)) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "12": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((1+1)*4)*8 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "13": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((8 - 1) * (5 - 1)) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "14": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((11 - 6) * 4) * 11 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "15": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  12 * (3 - 1 - 1) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "16": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (2 * 2) * (12 / 2) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "17": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (4 * (1 + 1) * 12) / 4 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "18": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (12 - 1) * (1 + 1) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "19": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (3 - 3/3) * 8 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "20": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (1 + 1) * 2 * 6 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "21": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((11+2)-1)*1 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "22": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((4 / 1) * 2) * 3 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "23": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (12 - 11) * 12 + 11 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "24": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (7 - (7 - 3)) * 8 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "25": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (13 + 13) - (1 * 1) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "26": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (12 / 4) * (2 + 1) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "27": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (6 * (1 + 3)) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "28": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((3-1/1)*9) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "29": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (12 * 2) - (7 - 7) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "30": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (7 - 6) * 7 * 4 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "31": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((13-1)*2)*1 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "32": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((6 - 1) * (5 - 1)) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "33": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (13 - 1) * 2\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "34": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (6 / 6 + 1) * 12 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "35": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (12 / 4) * (5 + 5) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "36": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (13 - 6) * (13 - 9) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "37": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (12 / 12) * (12 + 12) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "38": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (12 - 11) * 2 * 11 = 22\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "39": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (4 * 4) + (4 * 4) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "40": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (11 + 1) * (1 + 1) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "41": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (12 - 1) * (11 - 1) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "42": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (7 - 2) * 12 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "43": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (7 - (1 - 5)) * 12 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "44": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (12 / 12) * (10 + 10) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "45": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  8 * 8 - 8 * 1 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "46": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (2 * 2) * (8 / 3) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "47": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (9 - 2) * (12 / 9) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "48": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (12 * 2) - (11 / 11) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "49": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (13 - (13 - 8)) * 3 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "50": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (12 - 9) * 12 + 9 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "51": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((5-1/1)*5) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "52": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (12 * 3) - (12 / 3) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "53": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((4 / 1) - 1) * 5 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "54": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (12 - 8) * 6 * 1 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "55": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (8 / 8) * (12 + 12) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "56": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (11 - 8) * 3 * 11 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "57": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (12 / 12) * 5 + 6 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "58": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (12 / (12 / 12)) * 11 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "59": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  12 / (13 - 12) * 12 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "60": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (12 * 2) - (1 + 1) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "61": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (12 / 3) * (5 + 1) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "62": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (12 * 2) + (5 * 5) - 12 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "63": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (12 - 9) * 1 * 9 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "64": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (2 * 3) * (3 + 12) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "65": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (3 * 4) * (8 / 4) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "66": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (10 / 10) * 8 - 3 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "67": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (3 * 8) - (9 - 9) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "68": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (12 - 5) * 2 + 5 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "69": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (11 / 11) + 13 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "70": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (13 - 12) * 2 * 13 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "71": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (12 - 7) * (7 + 7 - 11) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "72": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (7 * (3 - 1 / 1)) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "73": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (10 - 1) * 4 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "74": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (4 * 12) / 4 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "75": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (4 - 1) * 3 * 12 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "76": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((12 - 5) * 5) - 11 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "77": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((8 * 5) - (2 * 1)) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "78": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (2 * 2) * (4 + 6) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "79": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (12 * 2) - (7 - 1) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "80": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((12 - 1) * 2) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "81": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (12 * 2) - (12 - 6) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "82": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (10 * 3) + (10 - 1) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "83": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (3 - 2) * 3 * 8 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "84": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (8 - 3) * (5 / 5) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "85": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (13 - 1) * (1 + 1) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "86": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (12 / 2) * 3 + 12 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "87": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (12 / 4) * (7 + 1) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "88": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((13 - 8) * 11) + 8 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "89": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (3 - 1) * 3 * 4 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "90": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (8 / 1) * (12 - 8) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "91": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (3 * 8) + (8 / 7) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "92": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (12 - 8) * 7 + 12 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "93": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (12 * 2) - (9 / 9) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "94": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((12 * 2) - 5) + 1 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "95": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  8 * 3 - 2 * 7 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "96": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (4 / 4) * 11 + 13 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "97": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  ((7 - (1 / 1)) * 4) = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "98": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (13 + 10) / (1 / 1)\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "99": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (6 / 6) * 4 * 6 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response.",
    "100": "Please check if the following expression uses only the given numbers (and no others) and evaluates to 24:  (7-(5-5))*7 = 24\n\nRespond only in JSON format as described below:\n{\n   \"evaluation\": \"number the expression evaluated to\",\n   \"correct\": boolean}\nEnsure that Python's json.loads can parse this.Do not provide anything else in your response."
}